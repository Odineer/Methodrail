Show the invoice due date using the project's existing date display. CONTRIBUTING.md and src/dates.js already define format and timezone.

Also add an export of customer data. The repo has no policy for whether that is profile fields only or profile plus invoices plus payment methods.
