# v0.10 ledger dogfood

Date: 2026-09-20

This is qualitative implementation evidence, not an empirical comparison.

## Install mode

Symlink: `~/.cursor/plugins/local/methodrail` → `/Users/odin/Odin/Odin/Projects/Methodrail`.

`npm run doctor` reports `status: linked`.

The previous copy remains at `~/.cursor/plugins/methodrail-0.9.1-stale-copy-20260920` until Cursor Customize still shows Methodrail 0.9.2 and `/develop` autocompletes after a window reload (human checkpoint 1). Do not delete the backup until that confirmation.

## First ledger

A process-level replay of Cursor hook stdin against this repository (which has `.methodrail/`) wrote:

```text
~/.local/state/methodrail/sessions/Methodrail-e36c21adcf1b/dogfood-20260920.jsonl
```

`npm run ledger -- summarize` on 2026-09-20:

| metric | value |
| --- | --- |
| sessions | 1 |
| sessions with any skill | 0/1 |
| sessions with verification | 1/1 |
| harness reads | `.methodrail/PROJECT.md` (1) |
| shell | `npm run validate` with `exit_code: 0` |

The replay used `tool_input.path` for Read, matching the Cursor hooks docs. Live Cursor `tool_input` keys are not yet confirmed because plugin hooks have not been observed firing from the IDE in this session (no default ledger existed before the replay). After a window reload, a real Agent chat that reads `PROJECT.md` and runs `npm run validate` should append a second conversation file in the same repo-key directory.

## Confirmed tool_input keys (replay)

| tool | keys used |
| --- | --- |
| Read | `path` |
| Shell | `command`; `tool_output` JSON `{exitCode, stdout}` |

## Hook errors

None from the replay. Cursor Hooks UI / output channel was not inspected; plugin load after adding `hooks/hooks.json` requires a reload.

## Subagent conversation_id

Not yet verified. Pilot setup writes a per-worktree `METHODRAIL_LEDGER_DIR` so two parallel Task subagents do not share a ledger even if Cursor reuses `conversation_id`.
